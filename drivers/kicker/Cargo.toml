[package]
name = "kicker"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
embedded-hal = "0.2.7"
defmt = "0.3.5"
defmt-macros = "0.3.5"
fugit = "0.3.7"

[features]
default = []
